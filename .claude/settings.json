{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r changed_file; npx prettier --write --ignore-unknown \"$changed_file\"; } 2>/dev/null || true",
            "statusMessage": "Formatting with prettier"
          }
        ]
      }
    ]
  }
}
